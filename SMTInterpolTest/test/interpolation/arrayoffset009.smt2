(set-option :produce-interpolants true)
(set-option :interpolant-check-mode true)
(set-logic QF_ALIA)
(set-info :status unsat)
(declare-const a (Array Int Int))
(declare-const b (Array Int Int))
(declare-const x Int)
(declare-const y Int)
(declare-const k Int)
;; const-weakeq: store writes the const value through the shifted equality y = x-1
(assert (! (= a ((as const (Array Int Int)) (+ x 1))) :named A))
(assert (! (and (= b (store a k (+ y 2))) (= y (- x 1)) (not (= b ((as const (Array Int Int)) (+ x 1))))) :named B))
(check-sat)
(get-interpolants A B)
(get-interpolants B A)
(exit)
